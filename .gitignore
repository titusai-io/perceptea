# Environment
.env
.env.local

# Build output
/bin/
/dist/
# The built binary at the repository root, and only that: a bare "perceptea"
# also matches the cmd/perceptea directory, which silently kept the service's
# entrypoint out of git while `go build ./...` passed because the package was
# not there to fail.
/perceptea

# Go
*.test
*.out
coverage.*

# Editors / OS
.DS_Store
.idea/
.vscode/
